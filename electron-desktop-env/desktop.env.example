# Copy to desktop.env for local npm run build:desktop (or let scripts/ensure-desktop-env.mjs create a stub).
# CI generates desktop.env from GitHub Actions secrets before electron-builder.
DATABASE_URL=
LICENSE_SIGNING_SECRET=
APP_BASE_URL=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
